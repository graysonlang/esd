{
  "name": "esd",
  "version": "0.1.0",
  "private": true,
  "description": "esbuild debugging",
  "keywords": [],
  "homepage": "https://github.com/graysonlang/esd#readme",
  "bugs": {
    "url": "https://github.com/graysonlang/esd/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/graysonlang/esd.git"
  },
  "license": "MIT",
  "author": "Grayson Lang",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "build:vscode": "node ./scripts/build.mjs --vscode",
    "debug:vscode": "node ./scripts/build.mjs --vscode --watch",
    "lint": "eslint . --ignore-pattern 'dist'",
    "serve": "node ./scripts/build.mjs --serve --proxy --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@graysonlang/esp": "^1.0.0"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^5.10.0",
    "esbuild": "^0.28.0",
    "eslint": "^9.39.4"
  }
}
